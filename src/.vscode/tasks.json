{
  "tasks": [
    {
      "type": "shell",
      "label": "g++.exe build active file",
      "command": "/usr/bin/g++",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "/usr/bin/"
      }
    },
    {
      "type": "shell",
      "label": "g++ build & run active file",
      "command": "/usr/bin/g++",
      "args": [
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "/usr/bin/"
      }
    }
  ],
  "version": "2.0.0"
}